# 星禾口语 · 产品需求文档（PRD.md）

> 版本：v1.2（Day 4，含自检修订与采纳的优化项）｜ 上游文档：research.md（Day 3）｜ 范围：Day 7 MVP「一心复读机 promax」
> 注：视频支持与 ima 式应用内文件夹管理为 Day 4 评审新增需求（research.md 未涉及，属范围扩充）。

## 1. 背景

目标用户（有老师带、老师发音频的口语学习者）目前依赖「一心复读机」等工具听练音频，但存在两大痛点：**文件管理混乱（或此功能收费）**、**VIP 广告频繁**。同类工具要么难用（Aboboo），要么偏内容平台且本地音频管理弱（每日英语听力）。星禾口语 = 原复读机核心功能 + 好用的文件夹管理 + 无广告，详见 research.md。

## 2. 目标用户

- **主要用户**：口语学习者——有老师带，老师会发整批学习音频/视频（一段段按内容分好），需要反复听、跟读。
- **使用场景**：拿到老师发来的一批音视频文件 → 导入工具 → 在应用内按自己的方式整理成文件夹 → 反复播放/复读练习。

## 3. 功能清单（本期做）

| # | 功能 | 说明 |
|---|---|---|
| F1 | **素材导入 + 应用内文件夹管理** | 核心卖点。支持导入**文件夹**（批量）或**单个文件**；重点：导入后可在**应用内直接新建、重命名、删除文件夹，移动素材归类**（类似 ima 知识库的组织方式），**不依赖本地原有的目录结构**——老师发的音频再乱，进来也能快速理清爽 |
| F2 | **音视频文件扫描/导入** | 扫描常见音频格式（mp3 / m4a / wav 等）和视频格式（mp4 / mov 等），自动识别导入 |
| F3 | **播放/暂停/快进快退（音视频通用）** | 基础播放控制：播放、暂停、快进、快退（快进/快退单次跳 5 秒）；音频和视频都支持 |
| F4 | **变速播放（音视频通用）** | 0.5–2.0 倍速可调，慢速精听、原速跟进；音频、视频均可用 |
| F5 | **单集循环 / AB 段复读（音视频通用）** | 可设定 A、B 两个点，反复播放区间；也可整段单集循环；音频、视频均可用 |
| F6 | **上次播放位置记忆** | 关掉再打开，自动回到上次听到/看到的地方接着听（音频、视频均适用） |
| F7 | **无广告** | 产品约束：全程零广告位、零 VIP 弹窗、零付费提示 |

## 4. 验收标准（每条均可亲手测试）

| # | 对应功能 | 验收标准（判定方法） |
|---|---|---|
| A1 | F1 | ① 导入一个含 **10 个以上文件**的文件夹，5 秒内全部显示；② 在应用内**新建 2 个文件夹**，把导入的素材**移动进去**，层级显示正确——全程不依赖本地目录结构；③ **重命名**其中一个文件夹、**删除**另一个空文件夹，列表更新正确且素材无丢失 |
| A2 | F2 | 扫描能识别 mp3 / m4a / wav / mp4 / mov；文件夹里的非媒体文件（如 pdf、图片）被忽略且**不报错、不显示** |
| A3 | F3 | 分别用**一个音频和一个视频**测试：点播放 **1 秒内出声/出画面**；暂停立即停；快进/快退**单次跳 5 秒**，点击后位置立即变化且继续正常播放 |
| A4 | F4 | 音频、视频均可在 0.5–2.0 倍间调节；**切换立即生效且播放不中断**；调回 1.0 倍声音/画面正常 |
| A5 | F5 | **单集循环**：开启后当前句/当前文件循环播放，关闭后恢复顺序播放；**AB 复读**：设定 A/B 两点后区间循环不止步，清除 A/B 后位置不错乱（音频、视频各测一次） |
| A6 | F6 | 音频、视频各播放中途关闭页面，重新打开后**自动定位到上次位置（误差不超过 3 秒）** |
| A7 | F7 | 从导入到播放全程：无任何广告弹窗、横幅、VIP/付费提示 |
| A8 | 全局 | **页面加载完成后断网**，F1–F6 全部可用（同时证明素材与数据不外传） |

## 5. 本期不做清单

| 功能 | 为什么本期不做 |
|---|---|
| 自动断句（按句切分音频） | 老师发的音频已按内容分段，收益低、技术难度高（用户 Day 4 拍板砍掉） |
| 班级管理系统（老师直发结构化音频） | 需要老师端 + 账号体系 + 服务器 |
| 同屏显示老师提示词 | 依赖班级管理先打通内容链路 |
| AB 段正字打卡评分、成绩汇报老师 | 需要录音评分引擎 + 后端存储 |
| 实时字幕 + 开字幕人数统计 | 需要语音识别与后端统计 |
| AI 分析音频、提取高频表达与模板 | 依赖 AI 接口与成本，MVP 之后再说 |
