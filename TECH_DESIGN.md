# 星禾口语 · 技术设计（TECH_DESIGN）

> 版本：v2.0（Day 6 调整）｜ 上游文档：PRD.md（Day 4）、research.md（Day 3）｜ 范围：Day 7 MVP「一心复读机 promax」
>
> **v2.0 变更记录**：应项目方向调整（手机端为主 + 独立 App 形态），技术路线由「纯前端 + 引用本地原文件」改为「**PWA + 统一复制存储**」。变更理由见第 1 节。

## 1. 技术路线

**选定：PWA（渐进式 Web App）+ 素材统一复制进应用存储**

两句话：
- **形态**：应用以网页技术构建，但用户访问一次后可「安装」到手机桌面和电脑开始菜单——点图标**全屏启动、无地址栏无浏览器按钮、断网可用**，用起来就是一个独立 App；
- **存储**：素材导入时**复制一份**进应用自己的存储（浏览器 IndexedDB），之后播放、整理都不再依赖原文件位置——手机电脑行为完全一致。

### 为什么这样选（对着两条新需求）

1. **「手机端为主、电脑端兼容」**：PWA 一套代码通吃两端。原路线 B 的核心能力（浏览器记住文件位置、直读硬盘）在手机浏览器上**不存在**，这是本次变更的直接原因。
2. **「独立 App 形态」**：PWA 安装后全屏启动、有自己的图标，体验上就是独立应用。若将来需要真正的安装包（`.apk` / `.exe`），可用 Capacitor（安卓）+ Electron（电脑）把同一份代码打包，**前期工作零浪费**——这是预留的升级路径，不在本期做（见第 5 节）。
3. **「统一复制」**：手机用户本来就习惯从微信/QQ 收文件后导入，复制导入符合直觉；且一套存储逻辑通吃两端，避免双份维护。代价是大视频导入需等待、占双份空间——音频场景下可接受。

### 已知代价与风险备案

| 代价 / 风险 | 说明 | 应对 |
|---|---|---|
| iPhone 限制较多 | iOS 对 PWA 的存储容量和后台能力有约束 | MVP 验收以安卓手机 + 电脑为主（与目标用户场景一致）；iPhone 实测列为待办 |
| 大视频导入等待 | 复制需要时间，视频越大越久 | 导入时显示进度提示（Day 7 实现细节）；音频文件小，不受影响 |
| 浏览器存储容量上限 | 手机浏览器对 IndexedDB 有配额限制 | 素材以老师发的一段段音频为主，量级可控；超出时提示用户清理 |

## 2. 前后端与数据库分工

| 角色 | 本项目的安排 | 说明 |
|---|---|---|
| 前端 | **星禾口语应用（唯一角色）** | 素材导入、应用内文件夹管理、播放/变速/AB 复读、位置记忆，全部由前端完成 |
| 后端 | **无** | MVP 不设后端。没有服务器，所以没有广告位（F7 天然成立），数据也无处可去 |
| 应用存储 | **浏览器 IndexedDB 充当「迷你数据库」** | 存三样东西：**素材副本**（复制的音视频文件本体）、应用内文件夹结构、播放进度记录 |

**这个分工的直接后果**：
- A8「页面加载后断网，全功能可用」成立——素材副本就在本机，一切不依赖网络；
- 远期扩展功能（班级管理、实时字幕统计、汇报老师）全部需要后端（多人的数据要汇到一处）——这正是它们被划入「本期不做」的技术根源。

## 3. 数据流

![数据流图](data-flow.svg)

**一句话说清数据从哪来、到哪去**：数据从「老师发来的本地音视频文件」来，导入时复制一份进应用存储，此后只在您的设备里流转（整理、播放、记进度），最后以画面和声音回到您的眼睛和耳朵——**全程不出网**。

## 4. 关键技术点（Day 7 实现要点）

- **PWA 三件套**：网页本体 + `manifest`（应用图标与名称，装到桌面用）+ Service Worker（离线缓存，断网可打开）；
- **IndexedDB**：浏览器自带迷你数据库，存素材副本、文件夹树、播放进度；
- **HTML5 audio / video**：浏览器自带的播放能力，变速用 `playbackRate`，AB 循环靠记录时间点；
- **纯静态页面**：无需服务器计算能力，部署简单（如 GitHub Pages）。

## 5. 后期升级路径（本期不做，仅记录方向）

- 真安装包：同一份代码用 **Capacitor** 打安卓 `.apk`、**Electron** 打电脑 `.exe`；
- 多端同步 / 班级管理：需要引入后端（届时同步修订本文件与 PRD 不做清单）。
